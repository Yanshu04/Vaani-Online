# =============================================================================
# Vaani Dependencies (Lightweight Cloud Deployment Config)
# =============================================================================

# --- Web & API Server ---
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.0.0
python-multipart>=0.0.9

# --- Audio Processing & File Handling ---
sounddevice>=0.4.6         # Captures live audio stream from the microphone (local only)
webrtcvad-wheels>=2.0.14   # Voice Activity Detection (local silence vs speech detection)
noisereduce>=3.0.2         # Cleans background noise from raw audio inputs
numpy>=2.1.0               # Performs matrix operations on audio arrays
soundfile>=0.12.1          # Reads and writes audio files for API communication

# --- Cloud APIs & Utilities ---
openai>=1.0.0              # OpenAI client used for Groq STT, LLM, and Translation APIs
python-dotenv>=1.0.1       # Loads environment configurations from local .env

# --- Text-to-Speech ---
pyttsx3>=2.90              # Offline text-to-speech engine (fallback)
edge-tts>=7.2.8            # Microsoft Edge neural TTS

# --- Testing ---
pytest>=8.1.1
